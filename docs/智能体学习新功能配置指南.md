# 智能体学习新功能配置指南

## 概述

本文档介绍如何让智能客服学习新的功能，例如查询数据库中的统计信息。

## 完整配置步骤

### 步骤一：后端添加 API

在对应的 Controller 中添加新的接口，用于获取统计数据。

**示例**：在 `AlarmConfigController.java` 中添加获取报警性质的 API

```java
/**
 * 获取报警性质种类数量
 * GET /api/alarm/response-req-count
 */
@GetMapping("/response-req-count")
public ResponseEntity<Map<String, Object>> getResponseReqCount() {
    try {
        List<String> responseReqList = alarmConfigService.getAllResponseReq();
        Map<String, Object> response = new HashMap<>();
        response.put("code", 200);
        response.put("message", "Success");
        response.put("data", new HashMap<String, Object>() {{
            put("count", responseReqList.size());
            put("types", responseReqList);
        }});
        return ResponseEntity.ok(response);
    } catch (Exception e) {
        logger.error("获取报警性质数量失败", e);
        Map<String, Object> errorResponse = new HashMap<>();
        errorResponse.put("code", 500);
        errorResponse.put("message", "获取报警性质数量失败: " + e.getMessage());
        return ResponseEntity.status(500).body(errorResponse);
    }
}
```

---

### 步骤二：放行 API（不需要登录授权）

在 `WebConfig.java` 的 `addInterceptors` 方法中添加新接口的放行路径。

```java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    registry.addInterceptor(authInterceptor)
            .addPathPatterns("/api/**")
            .excludePathPatterns(
                "/api/auth/login",
                "/api/auth/validate",
                "/api/batch-report/count",
                "/api/alarm/response-req-count"  // 新增的 API
            );
}
```

---

### 步骤三：在 RAG 服务中添加函数调用

在 `rag_service/main.py` 中完成以下三处修改：

#### 3.1 添加函数处理逻辑

在 `call_function` 函数中添加新的函数处理分支：

```python
elif function_name == "count_response_req":
    try:
        # 调用后端 API 获取报警性质种类数量
        response = requests.get("http://localhost:8080/api/alarm/response-req-count")
        if response.ok:
            data = response.json()
            if data.get("code") == 200:
                alarm_data = data.get("data", {})
                count = alarm_data.get("count", 0)
                types = alarm_data.get("types", [])
                types_str = "、".join(types) if types else "无"
                return f"报警配置系统共有 {count} 种报警性质，分别是：{types_str}。"
        return "无法获取报警性质数据"
    except Exception as e:
        return f"获取报警性质失败: {str(e)}"
```

#### 3.2 添加关键词匹配

在聊天接口中检查是否需要调用该函数：

```python
# 4. 检查是否需要获取报警性质
need_response_req = any(k in question for k in ['报警性质', '报警类型', '报警有哪些', '有几种报警'])
```

#### 3.3 调用函数并注入结果

```python
if need_response_req:
    # 获取报警性质统计
    response_req_info = call_function("count_response_req", {})

# ...

# 如果需要报警性质数据，将其注入
if need_response_req and response_req_info:
    messages.append({"role": "system", "content": f"\n\n重要实时数据：{response_req_info}"})
```

---

### 步骤四：在知识库中添加相关知识

在 `rag_service/init_knowledge.py` 中添加相关知识，帮助智能体理解该功能。

```python
# 报警配置系统
{
    "text": "报警配置管理系统用于设置设备报警规则，包含报警ID、报警内容、报警性质、处理方式等配置项。",
    "metadata": {"category": "报警功能", "source": "系统文档"}
},
{
    "text": "报警性质是报警配置中的一个重要字段，用于描述报警的类型或性质。系统支持多种不同的报警性质。",
    "metadata": {"category": "报警功能", "source": "系统文档"}
},
{
    "text": "报警性质有哪些？报警配置系统中存在多种不同的报警性质，具体的报警性质种类可以通过系统查询获取。",
    "metadata": {"category": "报警功能", "source": "系统文档"}
},
```

---

### 步骤五：重启服务使配置生效

#### 5.1 重启后端

重启 Spring Boot 应用，使新的 API 和 WebConfig 配置生效。

#### 5.2 重启 RAG 服务

```bash
# 1. 停止现有 Python 进程
taskkill /F /IM python.exe

# 2. 重新初始化知识库
cd d:/Study/test/rag_service
python init_knowledge.py

# 3. 启动 RAG 服务
python main.py
```

---

## 测试验证

### 测试后端 API

```bash
curl http://localhost:8080/api/alarm/response-req-count
```

预期返回：
```json
{
    "code": 200,
    "message": "Success",
    "data": {
        "count": 5,
        "types": ["火灾", "故障", "警告", "正常", "紧急"]
    }
}
```

### 测试 RAG 服务

```bash
curl -X POST http://localhost:8000/api/rag/chat \
  -H "Content-Type: application/json" \
  -d "{\"question\": \"有多少种报警性质\"}"
```

### 测试智能客服

在浏览器中打开前端页面，向智能客服提问：
- "有多少种报警性质？"
- "报警类型有哪些？"

---

## 快速添加新功能的模板

如果要添加一个新的统计功能（如：查询设备型号数量），按以下模板操作：

| 步骤 | 文件 | 关键点 |
|------|------|--------|
| 1 | Controller | 添加新 API，返回 `{count: N, items: [...]}` |
| 2 | WebConfig | 在 excludePathPatterns 添加新 API 路径 |
| 3 | main.py | 在 call_function 添加处理分支，添加关键词匹配，注入结果 |
| 4 | init_knowledge.py | 添加 2-3 条相关知识 |
| 5 | 重启 | 重启后端和 RAG 服务 |

---

## 注意事项

1. **关键词要全面**：考虑用户可能的多种问法，如"多少种"、"有哪些"、"数量"等
2. **知识库要多样**：同一条知识用不同的问法多写几条，提高匹配成功率
3. **API 要返回中文**：call_function 中拼装返回字符串时要使用中文
4. **放行路径**：新增的 API 必须在 WebConfig 中放行，否则 RAG 服务无法调用
